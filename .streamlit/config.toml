[theme]
primaryColor = "#E7BA4D"           # Colore principale (bottoni, slider)
backgroundColor = "#0E1117"        # Sfondo principale
secondaryBackgroundColor = "#262730"  # Sfondo secondario (sidebar, container)
textColor = "#FAFAFA"              # Colore del testo
font = "sans serif"                # Font: "sans serif", "serif", o "monospace"